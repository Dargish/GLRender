#pragma once
#include <core/fwd.h>

namespace graphics
{
	DECL_CLASS(Camera);
	DECL_CLASS(Cube);
	DECL_CLASS(Material);
	DECL_CLASS(Mesh);
	DECL_CLASS(Primitive);
	DECL_CLASS(ShaderValue);
	DECL_CLASS(Shader);
	DECL_CLASS(Transform);
	DECL_CLASS(Window);
}